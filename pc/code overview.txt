PC:

libraries:
 · serial port
 · wave parser

./send song.wav


define send_size

main
	stm32.connect()
	passes filename into Wave
	assert bit depth and sample rate
	get pointer to wave sample data
	get size of sample data
	status(stm32)
	while status.isready {
		sends i++th sample
		loop song as needed
	}




class status:

status(stm32)
	new thread status_listener

bool Ready

function status_listener()
	while true, updates Ready
